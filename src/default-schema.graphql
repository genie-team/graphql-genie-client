# This is sample IDL schema for GraphQL Genie.
#

type Company {
	id: ID!
  name: String
  industry: String
  employees: [Employee!]
}

type Employee {
	id: ID!
  firstName: String
  lastName: String
  address: String
  subordinates: [Employee!]
  company: Company
}
